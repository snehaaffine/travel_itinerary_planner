# Copy to backend/.env and frontend/.env respectively.
# See README.md for full documentation.

# Backend defaults (see backend/.env.example)
DATABASE_URL=postgresql+psycopg://travel_planner:travel_planner@localhost:5433/travel_planner
REDIS_URL=redis://localhost:6379/0
APP_API_TOKEN=dev-app-token

# Frontend defaults (see frontend/.env.example)
VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_APP_API_TOKEN=dev-app-token
